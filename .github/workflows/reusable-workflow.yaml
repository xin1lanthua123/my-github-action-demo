name: Reusable Workflow
on:
    workflow_call:
        
jobs:
    greeting:
        runs-on: ubuntu-latest
        steps:
            - name: Greeting
              run: |
                echo "Welcome to reusable workflow"
  
        
        