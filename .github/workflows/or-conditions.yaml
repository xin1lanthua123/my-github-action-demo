name: Or Condition

on:
  push:
    branches:
      - main
      - feature/demo

jobs:
  demo:
    runs-on: ubuntu-latest
    env:
        repo: ${{github.actor}}
    steps:
      - name: print github actor
        run: |
          echo "${{github.actor}}"
          
      - name: Run for Javahometech or Hari
        if:  github.actor == 'HARI'|| github.actor == env.repo
        run: |
            echo "running this step as succeed"

    